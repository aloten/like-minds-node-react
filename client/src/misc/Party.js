import party from './party.gif';

const Party = () => {
  return (
    <div>
      <img
        src={party}
        style={{ width: '200px', margin: 'auto', display: 'block' }}
        alt='Loading...'
      />
    </div>
  );
};

export default Party;
